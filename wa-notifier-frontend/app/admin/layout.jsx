// Nested layout for the /admin area (platform Admin Dashboard) — see
// master/layout.jsx for why this is just a pass-through.
export default function AdminLayout({ children }) {
  return children;
}
