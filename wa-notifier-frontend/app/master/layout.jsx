// Nested layout for the /master area (platform messaging workspace) — see
// master/layout.jsx for why this is just a pass-through.
export default function ClientAreaLayout({ children }) {
  return children;
}
